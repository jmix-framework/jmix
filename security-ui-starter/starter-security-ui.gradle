apply plugin: 'java'
apply plugin: 'java-library'

group = 'io.jmix.starter.security'

dependencies {
    api project(':modules:security')
    api project(':modules:security-ui')

    api 'org.springframework.boot:spring-boot-autoconfigure'
}

jar {
    archivesBaseName = 'jmix-starter-security-ui'
}

java {
    withSourcesJar()
}

artifacts {
    archives sourcesJar
}

publishing {
    publications {
        javaMaven(MavenPublication) {
            artifactId = 'jmix-starter-security-ui'
            from components.java
        }
    }
}
